# Feature Matrix

> Generated from `docs/internal/feature-status.json` by `scripts/render-feature-status.mjs`. Do not edit this file by hand.

Quick-scan summary for planning and safe edits. For full evidence and notes, use [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md).

| Feature | Status | Surface Type | Public Surface | Owner Area | Backing | Canonical Details |
| --- | --- | --- | --- | --- | --- | --- |
| ChatEngine orchestration | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Engine lifecycle, message CRUD, transport wiring, storage integration, plugin registration, and middleware execution are implemented and tested. |
| Event system | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Typed core events plus custom namespaced events are present and tested. |
| Streaming helper methods | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Core exposes explicit stream lifecycle helpers, but first-party adapters do not automatically emit them. |
| Typing indicators | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Core provides ephemeral conversation-scoped typing state, typed transport payloads, TTL expiry, throttled outbound emits, and selectors without persisting typing history. |
| Storage contract (IStorage) | `interface-only` | `public-sdk` | yes | @kaira/chat-core | `contract-only` | The public contract remains exported from core, and the repo now ships a first-party browser implementation in `@kaira/chat-storage-indexeddb`. |
| ChatEngine storage integration | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Storage-backed get, save, update, delete, and dedupe behavior is implemented inside core. |
| Provider abstraction (IProvider) | `interface-only` | `public-sdk` | yes | @kaira/chat-core | `contract-only` | The provider contract is exported, but there is no concrete provider package or engine integration in the repo. |
| Middleware pipeline | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Middleware registration and ordered execution are implemented and tested, but there is no first-party middleware package. |
| Plugin contract and lifecycle | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Engine-level plugin registration and teardown exist, but there are no first-party plugin packages. |
| IndexedDB storage adapter | `implemented` | `public-sdk` | yes | @kaira/chat-storage-indexeddb | `concrete` | The repo now includes a browser-first IndexedDB `IStorage` adapter with lazy database open, serializer-backed records, message and conversation pagination, and in-memory fallback when IndexedDB is unavailable. |
| Polling transport | `implemented` | `public-sdk` | yes | @kaira/chat-transport-polling | `concrete` | Generic polling-based ITransport implementation with reconnect/backoff behavior is present and tested. |
| DIT transport adapter | `implemented` | `public-sdk` | yes | @kaira/chat-provider-dit | `concrete` | This package implements a transport adapter, not a concrete IProvider. The package name is easy to over-read. |
| WebSocket transport | `implemented` | `public-sdk` | yes | @kaira/chat-transport-websocket | `concrete` | A generic first-party WebSocket ITransport implementation now ships with configurable frame parsing, outbound serialization, connection lifecycle handling, and reconnect behavior. |
| React provider and hooks | `implemented` | `public-sdk` | yes | @kaira/chat-react | `concrete` | The exported React runtime is source-backed and used by the demo app. |
| React streaming hook | `implemented` | `public-sdk` | yes | @kaira/chat-react | `concrete` | The hook is implemented against core stream events and is now exercised by the local apps/web streaming demos over real stream lifecycle events. |
| React typing hooks | `implemented` | `public-sdk` | yes | @kaira/chat-react | `concrete` | Conversation-scoped typing state, participant selectors, boolean selectors, and composer controls are exported and covered by focused hook tests. |
| Default UI renderers | `implemented` | `public-sdk` | yes | @kaira/chat-ui | `concrete` | Built-in renderers cover the SDK's first-class message types: text, ai text-stream, image, audio, video, file, location, and system. Provider-specific transcript payloads remain out of scope for default renderer support and should use custom message renderers. |
| Typing indicator UI primitive | `implemented` | `public-sdk` | yes | @kaira/chat-ui | `concrete` | The UI package now ships a reusable participant-aware typing indicator plus composer callbacks for draft and blur events without coupling the component to engine state. |
| Devtools panel | `implemented` | `public-sdk` | yes | @kaira/chat-devtools | `concrete` | Runtime inspection UI is implemented, but automated coverage is absent. |
| Demo browser runtime | `implemented` | `demo-app` | no | apps/web client | `concrete` | The browser runtime is now created per demo, persists locally with IndexedDB, and targets a namespaced demo route surface instead of one global client singleton. |
| Demo server runtime | `implemented` | `demo-app` | no | apps/web server | `concrete` | apps/web now exposes a generalized demo backend registry that serves both the DIT-backed runtime and several local server-owned ChatEngine demos. |
| Demo streaming UX | `implemented` | `demo-app` | no | apps/web | `concrete` | Local Next-backed demos now emit real stream lifecycle events and bridge them into the browser runtime over the demo SSE route, while the DIT path remains non-streaming. |
| Demo typing indicators | `implemented` | `demo-app` | no | apps/web | `concrete` | Typing indicators remain transport-backed, and the local demos now also synthesize assistant typing through the server runtime for easier verification. |
| SSE events route | `implemented` | `demo-app` | no | apps/web API | `concrete` | The generalized demo events route still supports polling for messages and typing, and local stream-capable demos now consume its SSE branch through an EventSource bridge. |
| WebSocket demo showcase | `implemented` | `demo-app` | no | apps/web | `concrete` | apps/web now includes a local-only WebSocket showcase that bootstraps the conversation over the existing HTTP demo routes, then drives message and typing traffic through the first-party WebSocket transport with a manual reconnect control. |
| Conversation bootstrap route | `implemented` | `demo-app` | no | apps/web API | `concrete` | Demo routes now actively bootstrap their conversation state from the server route before rendering the chat surface. |
| Consumer docs app | `implemented` | `consumer-docs` | no | apps/docs | `concrete` | The docs app is source-backed and deployed, but it is authored MDX rather than generated API reference material. |
| Docs search index | `partial` | `consumer-docs` | no | apps/docs | `concrete` | The committed search index is generated from authored MDX and now guarded by integrity tests, but regeneration is still a separate manual step rather than an explicit root workflow. |
| Internal feature status system | `implemented` | `internal-docs` | no | docs/internal | `concrete` | The canonical source lives in JSON and generates the markdown views to reduce drift between status documents. |
| CI validate workflow | `implemented` | `tooling-release` | no | root workflows | `workflow-only` | CI runs the repo-native validate command on pull requests and pushes to main. |
| Docs deployment workflow | `implemented` | `tooling-release` | no | root workflows | `workflow-only` | GitHub Pages deployment for apps/docs is active and source-backed. |
| Package release automation | `partial` | `tooling-release` | no | root workflows | `workflow-only` | Changesets configuration exists, but the checked-in release workflow does not currently run its publish path because the trigger and job guard do not line up. |
| @kaira/chat-core automated tests | `implemented` | `public-sdk` | yes | @kaira/chat-core | `concrete` | Core runtime behavior has broad unit coverage across engine, events, state, middleware, serialization, and utilities. |
| First-party adapter automated tests | `implemented` | `public-sdk` | yes | storage and transport adapters | `concrete` | IndexedDB storage, polling transport, and DIT transport adapter behavior are covered by focused Vitest suites. |
| @kaira/chat-react automated tests | `implemented` | `public-sdk` | yes | @kaira/chat-react | `concrete` | The React runtime now has focused behavioral coverage for provider lifecycle, connection state, conversation and message subscriptions, streaming hooks, send delegation, and optimistic reconciliation. |
| @kaira/chat-ui and @kaira/chat-devtools automated tests | `implemented` | `public-sdk` | yes | UI and devtools packages | `concrete` | @kaira/chat-ui and @kaira/chat-devtools now have focused behavioral coverage for renderer fallback, typing and composer primitives, runtime capture, engine-swap resets, and panel rendering. |
| apps/web automated tests | `implemented` | `demo-app` | no | apps/web | `concrete` | apps/web now has focused Vitest coverage for the WebSocket bridge helpers, the demo reconnect control route, and the central demo registry alongside the existing route and runtime tests. |
| apps/docs automated tests | `implemented` | `consumer-docs` | no | apps/docs | `concrete` | The docs app now has lightweight integrity coverage for the generated search index and the critical top-level docs routes it must expose. |

Status source of truth: [`feature-status.json`](./feature-status.json)

